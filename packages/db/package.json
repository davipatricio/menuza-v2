{
  "name": "@menuza/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    "./package.json": "./package.json",
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "db:deploy": "bun --env-file-if-exists=../../.env run prisma migrate deploy",
    "db:generate": "bun --env-file-if-exists=../../.env run prisma generate",
    "db:migrate": "bun --env-file-if-exists=../../.env run prisma migrate dev",
    "db:studio": "bun --env-file-if-exists=../../.env run prisma studio",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/orm-postgres": "^8.0.0-rc.10",
    "pg": "8.13.1"
  },
  "devDependencies": {
    "@oxlint/plugins": "catalog:",
    "@types/node": "catalog:",
    "@types/pg": "8.11.10",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "prisma": "^8.0.0-rc.14",
    "typescript": "catalog:"
  },
  "prisma": {
    "seed": "bun run prisma/seed.ts"
  }
}
